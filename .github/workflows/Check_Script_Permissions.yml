name: Check Script Permissions

on:
  push:
    branches:
      - main

jobs:
  check_permissions:
    runs-on: macos-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: List file permissions
      run: ls -l $AGENT_HOMEDIRECTORY/scripts/select-xamarin-sdk.sh
